// import * as actions from './actions'

const initialState = {
  // TODO: Set initial state
}

var AppReducer = (state, action) => {
  state = state || initialState

  // TODO: Create and add actions

  return state
}

export default AppReducer
